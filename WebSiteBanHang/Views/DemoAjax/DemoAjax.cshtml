@{
    ViewBag.Title = "DemoAjax";
    Layout = "~/Views/Layout/HomeLayout.cshtml";
}

@*Load ajax theo cách thứ 1 dùng Ajax.ActionLink*@

@*@Ajax.ActionLink("Load Ajax MVC", "LoadAjaxActionLink", new AjaxOptions {HttpMethod="GET",UpdateTargetId="KetQua",InsertionMode=InsertionMode.Replace,LoadingElementId="divLoading" })*@


@*Load ajax theo cách thứ 2 dùng Ajax.Begiform*@
@*@using (Ajax.BeginForm("LoadAjaxBeginForm", new AjaxOptions { HttpMethod = "Post", UpdateTargetId = "KetQua", InsertionMode = InsertionMode.Replace, LoadingElementId = "divLoading" }))
    {

        <input name="txt1" />
        <input type="submit" value="Submit" />
    }*@

@*<input type="button" id="button" value="Submit Jquery Ajax" />

    <div id="divLoading" style="display:none;">
        <img src="~/Content/images/animatedTrapezoids.gif" />
    </div>

    <input id="txt1" />
    <input id="txt2" />

    <div id="KetQua" style="height:150px;">
        Đây là kết quả trả về
        <br />

    </div>*@

@*Load ajax theo cách thứ 3 dùng Jquery*@
@*<script>
        $("#button").click(function () {
            $('#divLoading').show();
            var thamso1 = $("#txt1").val();
            var thamso2 = $("#txt2").val();
            $.ajax({
                type: 'GET',
                data: {
                    a: thamso1,
                    b: thamso2
                },
                url: '@Url.Content("LoadAjaxJQuery")',
                success: function (ketqua) {
                    $('#divLoading').hide();
                    $('#KetQua').html(ketqua);
                }
            })
        })
    </script>*@

@*Load partialview Ajax*@
@Ajax.ActionLink("Load Ajax MVC", "LoadSanPhamAjax", new AjaxOptions { HttpMethod = "GET", UpdateTargetId = "KetQua", InsertionMode = InsertionMode.Replace, LoadingElementId = "divLoading" })
<div class="container">
    <div class="content-top">
        <div id="KetQua" style="height:150px;">
            Đây là kết quả trả về
            <br />

        </div>
        </div>
    </div>
        <div id="divLoading" style="display:none;">
            <img src="~/Content/images/animatedTrapezoids.gif" />
        </div>
